# Generated by scripts/bake_server_art.sh. Its lines are single base64 data
# URIs — prettier wraps them, which makes the script's --check report drift on
# every formatted checkout. The script owns the file's shape.
src/lib/companion-art.ts
